linkedin-scraper>=3.1.1
playwright>=1.58.0
playwright-stealth>=2.0.0
openpyxl>=3.1.5
python-dotenv>=1.2.2
requests>=2.31.0

# ── Gestion des documents Word ───────────────────────────────────
python-docx>=1.1.0

# ── Conversion Word → PDF (requiert Microsoft Word sur Windows) ──
docx2pdf>=0.1.8
# pywin32 est le fallback COM si docx2pdf échoue.
# Installez-le manuellement si nécessaire : pip install pywin32
# pywin32>=306

# ── API Claude (Anthropic) ───────────────────────────────────────
anthropic>=0.25.0
